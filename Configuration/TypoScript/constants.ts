
module.tx_contentimporter_contentimport {
  view {
    # cat=module.tx_contentimporter_contentimport/file; type=string; label=Path to template root (BE)
    templateRootPath =
    # cat=module.tx_contentimporter_contentimport/file; type=string; label=Path to template partials (BE)
    partialRootPath =
    # cat=module.tx_contentimporter_contentimport/file; type=string; label=Path to template layouts (BE)
    layoutRootPath =
  }
  persistence {
    # cat=module.tx_contentimporter_contentimport//a; type=string; label=Default storage PID
    storagePid =
  }
}
